# Base URL of the Node.js API.
# SAFE to expose: it is only an address. NEVER put Google credentials
# or the admin key in any VITE_ variable - they ship to every browser.
VITE_API_URL="http://localhost:5000"
